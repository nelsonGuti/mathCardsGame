import { FlipCardFrontDirective } from './flip-card-front.directive';

describe('FlipCardFrontDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipCardFrontDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
